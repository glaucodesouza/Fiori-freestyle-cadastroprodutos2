<mvc:View
    controllerName="fiorinet.cadastroprodutos3.controller.Worklist"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic">

    <semantic:SemanticPage
        id="page"
        headerPinnable="false"
        toggleHeaderOnTitleClick="false">

        <semantic:titleHeading>
            <Title
                text="{i18n>worklistTitle}"
                level="H2"/>
        </semantic:titleHeading>

        <semantic:content>
            <Table
                id="table"
                width="auto"
                mode="MultiSelect"
                items="{
                    path: '/Z270CADPRODUTOSSet',
                    sorter: {
                        path: 'Codigo',
                        descending: false
                    }
                }"
                noDataText="{worklistView>/tableNoDataText}"
                busyIndicatorDelay="{worklistView>/tableBusyDelay}"
                growing="true"
                growingScrollToLoad="true"
                updateFinished=".onUpdateFinished">

                <headerToolbar>
                    <OverflowToolbar>
                        <Title
                            id="tableHeader"
                            text="{worklistView>/worklistTableTitle}"
                            level="H3"/>
                        <ToolbarSpacer />
                        <Button id="btnCriar" text="Criar" press="onCriar"/>
                        <Button id="btnUpdate" text="Modificar" press="onUpdate"/>
                        <SearchField
                            id="searchField"
                            tooltip="{i18n>worklistSearchTooltip}"
                            search=".onSearch">
                            <layoutData>
                                <OverflowToolbarLayoutData
                                    maxWidth="200px"
                                    priority="NeverOverflow"/>
                            </layoutData>
                        </SearchField>
                    </OverflowToolbar>
                </headerToolbar>

                <columns>
                    <Column id="codigoColumn">
                        <Text text="Código" id="codigoColumnLabel"/>
                    </Column>
                    <Column id="descricaoColumn" hAlign="End">
                        <Text text="Produto" id="descricaoColumnLabel"/>
                    </Column>
                    <Column id="kwmengColumn" hAlign="End">
                        <Text text="Quantidade" id="kwmengColumnLabel"/>
                    </Column>
                    <Column id="netprColumn" hAlign="End">
                        <Text text="Preço Líquido" id="netprColumnLabel"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem
                        type="Navigation"
                        press=".onPress">
                        <cells>
                            <ObjectIdentifier title="{Codigo}"/>
                            <Text text="{Descricao}"/>
                            <ObjectNumber number="{
                                    path: 'Kwmeng',
                                    formatter: '.formatter.numberUnit'
                                }"
                                unit="{Meins}" />
                            <ObjectNumber number="{
                                    path: 'Netpr',
                                    formatter: '.formatter.currencyUnit'
                                }"
                                unit="{Waerk}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </semantic:content>

        <semantic:sendEmailAction>
            <semantic:SendEmailAction id="shareEmail" press=".onShareEmailPress"/>
        </semantic:sendEmailAction>

    </semantic:SemanticPage>

</mvc:View>